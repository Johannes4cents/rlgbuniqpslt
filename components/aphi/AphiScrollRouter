// components/aphi/AphiScrollRouter.tsx

import React from "react";

type Props = {
  slug: string[];
};

export const AphiScrollRouter: React.FC<Props> = ({ slug }) => {
  return (
    <div style={{ padding: "2rem", fontFamily: "monospace" }}>
      <h2>🌀 AphiScrollRouter</h2>
      <p>This scroll responds to the path segments:</p>
      <pre>{JSON.stringify(slug, null, 2)}</pre>
    </div>
  );
};
// components/aphi/AphiScrollRouter.tsx

import React from "react";

type Props = {
  slug: string[];
};

export const AphiScrollRouter: React.FC<Props> = ({ slug }) => {
  return (
    <div style={{ padding: "2rem", fontFamily: "monospace" }}>
      <h2>🌀 AphiScrollRouter</h2>
      <p>This scroll responds to the path segments:</p>
      <pre>{JSON.stringify(slug, null, 2)}</pre>
    </div>
  );
};
export default AphiScrollRouter;
